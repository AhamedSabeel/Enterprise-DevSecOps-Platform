apiVersion: v1
kind: Service
metadata:
  name: backend-blue-green
  namespace: enterprise-devsecops
spec:
  selector:
    app: backend
    version: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
