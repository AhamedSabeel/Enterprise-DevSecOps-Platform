apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: enterprise-devsecops

spec:
  selector:
    app: auth-service

  ports:
    - name: http
      protocol: TCP
      port: 8001
      targetPort: 8001

  type: ClusterIP
